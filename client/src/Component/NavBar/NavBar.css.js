import { makeStyles } from '@material-ui/core';

export default makeStyles((theme) => ({
    header: {
        background: '#317947'
    },
    tabs: {
        color: '#e2e4e2',
        marginRight: 20,
        textDecoration: 'none',
        fontSize: 20
    }
}));